// Знаходимо кнопку по ID
const button = document.getElementById("mainBtn");

// Вішаємо обробник події "click"
button.addEventListener("click", function() {

    // Коли кнопка натиснута — показуємо повідомлення
    alert("Дякуємо! Ми скоро з вами зв’яжемося ❤️");
});
